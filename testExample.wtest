import armas.*
import elementos.*
import jugadora-y-personajes.*

//describe "El programa debe resolver el encuentro entre un personaje y un elemento" {
  test "Mario es feliz luego de encontra un elemento" {
    mario.encontrar(castillo)
    assert.that( mario.esFeliz())
  }
